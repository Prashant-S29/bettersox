{
  "ignoreCommand": "bash -c 'if [[ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" && \"$VERCEL_GIT_COMMIT_REF\" != \"dev\" && \"$VERCEL_GIT_COMMIT_REF\" != \"staging\" ]]; then exit 0; else exit 1; fi'",
  "crons": [
    {
      "path": "/api/cron/check-trackers",
      "schedule": "*/5 * * * *"
    },
    {
      "path": "/api/queue/send-email",
      "schedule": "*/2 * * * *"
    }
  ]
}